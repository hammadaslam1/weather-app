import { AppBar, Box } from "@mui/material";

const Navbar = () => {
    return ( 
        <Box>
            <AppBar position="sticky">
                
            </AppBar>
        </Box>
     );
}
 
export default Navbar;